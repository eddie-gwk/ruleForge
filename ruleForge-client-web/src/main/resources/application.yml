server:
  port: 8868
spring:
  application:
    name: ruleForge-client-web
dubbo:
  application:
    id: ruleForge-client-web-provider
    name: ruleForge-client-web-provider
  provider:
    version: 1.0
  protocol:
    name: dubbo
    port: 28880
  registry:
    id: ruleForge-client-web-registry
    address: zookeeper://127.0.0.1:2181
    timeout: 20000
    simplified: true
  metadata-report:
    address: zookeeper://127.0.0.1:2181
  scan:
    base-packages: com\yunext\storage\provider
  consumer:
    check: false
    version: 1.0
    timeout: 5000

